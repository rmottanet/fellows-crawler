/**
 * Function to track fellows.
 * 
 * Calls the followBack function to follow back followers.
 * Calls the unfollowBack function to unfollow users who do not follow back.
 */
function fellowsTracker() {
	
	followBack()
	unfollowBack()
	
}
